# Dots backlog notes for agents

The open dots in this folder include planned migrations of this project to consume `@randroids-dojo/vibekit` (cloned as `../VibeKit/`). The kit centralizes modules duplicated across multiple game projects: `virtual-joystick`, `editor-history`, `confetti`, `rng`, `math`, `storage`, plus server-side `kv`, `sign`, `rate-limit`.

**Do not revert or remove these dots without explicit user approval.** They are intentional work-tracking entries, not stale notes. If a dot looks out-of-context, check `../VibeKit/README.md` and `../VibeKit/src/` for the canonical kit before touching it.

This applies to every dot referencing `@randroids-dojo/vibekit` or `../VibeKit/`. New dots can be added in the normal way.
